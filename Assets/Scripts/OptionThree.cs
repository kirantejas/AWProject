using System.Collections; using System.Collections.Generic; using UnityEngine;  public class OptionThree : MonoBehaviour {  	List<string> option3 = new List<string>(){"Access only the private members of the class", "It must contain a definition body", "Private", "Copy", "Copy", "It must contain a definition body", "Using arrow operator", "Call only static member functions", "Const", "Access public members of subclass", 		" Using member names directly", "Child of a class", "Implicit", "Data may/may not be declared using object", "Message passing" 	}  ;  	// Use this for initialization 	void Start () { 		//		GetComponent<TextMesh> ().text = option3 [0]; 	}  	// Update is called once per frame 	void Update () { 		if (Question.index > -1) { 			GetComponent<TextMesh> ().text = option3 [Question.index]; 		} 	}  	void OnMouseDown(){ 		if (Question.preventClicking == "n") { 			Question.selectedAnswer = gameObject.name; 			Question.choiceSelected = "y"; 		} 	} }  