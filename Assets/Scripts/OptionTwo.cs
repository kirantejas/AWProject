using System.Collections; using System.Collections.Generic; using UnityEngine;  public class OptionTwo : MonoBehaviour {   	List<string> option2 = new List<string>(){"Access only Public members of the class", "It must not have any return type", "Protected", "Parameterized", "Parameterized", "It must not have any return type", "Using dot operator", "Call only private member functions", "Constructor", "Definition without return type", 		"Using scope resolution operator", "Instance of itself", "Protected", "Inheritance", "Data binding" 	}  ;  	// Use this for initialization 	void Start () { 		//		GetComponent<TextMesh> ().text = option2 [0]; 	}  	// Update is called once per frame 	void Update () { 		if (Question.index > -1) { 			GetComponent<TextMesh> ().text = option2 [Question.index]; 		} 	}  	void OnMouseDown(){ 		if (Question.preventClicking == "n") { 			Question.selectedAnswer = gameObject.name; 			Question.choiceSelected = "y"; 		} 	} }  