using System.Collections; using System.Collections.Generic; using UnityEngine;  public class OptionFour : MonoBehaviour {  	List<string> option4 = new List<string>(){"Access subclass members", "It can contains arguments", "Any access specifier will work", "Direct assignment is used", "Constructors don’t have a return type", "It can contains arguments", "Used directly or with this pointer", "Call only const member functions", "Constructor and destructor", "Access static members of class", 		"Using pointer only", "Overview of a class", "Explicit", "Functions Overloading", "Data hiding" 	}  ; 	// Use this for initialization 	void Start () { 		//		GetComponent<TextMesh> ().text = option4 [0]; 	}  	// Update is called once per frame 	void Update () { 		if (Question.index > -1) { 			GetComponent<TextMesh> ().text = option4 [Question.index]; 		} 	}  	void OnMouseDown(){ 		if (Question.preventClicking == "n") { 			Question.selectedAnswer = gameObject.name; 			Question.choiceSelected = "y"; 		} 	} }  